# Ignore all data files (contain sensitive/large data)
*

# But keep the directory structure
!.gitignore
!redis/.gitkeep
!postiz/.gitkeep
!postiz/config/.gitkeep
!postiz/uploads/.gitkeep
# Note: postgres uses a Docker named volume (eb24-postiz-postgres-data)
